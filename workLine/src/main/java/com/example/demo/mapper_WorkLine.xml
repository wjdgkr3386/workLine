<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.WorkLineDAO">

	<select id="checkUuid" parameterType="com.example.demo.WorkLineDTO" resultType="int">
		select count(uuid) from member where uuid=#{uuid}
	</select>
	
	<select id="checkMid" parameterType="com.example.demo.WorkLineDTO" resultType="int">
		select count(mid) from member where mid=#{mid}
	</select>

	<select id="login" parameterType="com.example.demo.WorkLineDTO" resultType="int">
		select count(*) from member where mid=#{mid} and pwd=#{pwd}
	</select>

	<select id="getUuid" parameterType="com.example.demo.WorkLineDTO" resultType="String">
		select uuid from member where mid=#{mid} and pwd=#{pwd}
	</select>
	
	<select id="getMyInfo" parameterType="String" resultType="java.util.HashMap">
		select
			uuid as "UUID",
			name as "NAME",
			mid as "MID",
			pwd as "PWD",
			phone as "PHONE",
			img as "IMG"
		from
			member
		where
			uuid=#{uuid}
	</select>
	
	<select id="getFriend" parameterType="String" resultType="java.util.HashMap">
		select
			uuid as "FRIENDCODE",
			name as "NAME",
			phone as "PHONE",
			img as "IMG"
		from
			member
		where
			uuid in (select friendCode from friend where uuid=#{uuid})
	</select>
	
	<select id="checkChatRoomDirect" parameterType="java.util.HashMap" resultType="int">
		select count(*) from chat_room_direct where uuid=#{uuid} and friendCode=#{friendCode}
	</select>
	
	<select id="isFriend" parameterType="java.util.HashMap" resultType="int">
		select count(*) from friend where uuid=#{friendCode}
	</select>
	
	<select id="getChatRoomDirect" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		select 
	</select>

	<insert id="insertSignup" parameterType="com.example.demo.WorkLineDTO">
		insert into member (
			uuid,
			name,
			mid,
			pwd,
			phone
		) values (
			#{uuid},
			#{name},
			#{mid},
			#{pwd},
			concat(#{phone1},#{phone2},#{phone3})
		)
	</insert>
	
 	<insert id="insertChat" parameterType="com.example.demo.WorkLineDTO">
		insert into chat_message () values ()
	</insert>
	
 	<insert id="insertFriend" parameterType="java.util.HashMap">
		insert into friend (uuid, friendCode) values (#{uuid}, #{friendCode})
	</insert>
	
 	<insert id="insertChatRoomDirect" parameterType="java.util.HashMap">
		insert into chat_room_direct (uuid, friendCode) values (#{uuid}, #{friendCode})
	</insert>
	
	
</mapper>